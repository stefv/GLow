<Button x:Class="GLow_Screensaver.Controls.ReduiceBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GLow_Screensaver.Controls"
             mc:Ignorable="d"
             d:DesignHeight="25" d:DesignWidth="37">

    <Button.Resources>
        <Style x:Key="CloseBoxStyle" TargetType="{x:Type Grid}">
            <Setter Property="Background" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF7F7F7F"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Button.Resources>

    <Grid Style="{StaticResource CloseBoxStyle}">
        <TextBlock Text="_" HorizontalAlignment="Center" VerticalAlignment="Center" />
    </Grid>
</Button>
